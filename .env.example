# API key for meal plan generation (required for /api/generate)
# Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=your_key_here

# Optional: use mock data instead of API (no key needed)
# VITE_USE_MOCK=true
